from django.apps import AppConfig


class AccountsConfig(AppConfig):
    """Configuration for the 'accounts' app in your Django project."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "v1.accounts"
